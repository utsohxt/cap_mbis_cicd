{
  "name": "equipment",
  "version": "1.0.0",
  "private": true,
  "description": "Equipment Fiori App",
  "devDependencies": {
    "@ui5/cli": "^3"
  },
  "scripts": {
    "build": "ui5 build --config ui5-deploy.yaml --clean-dest --dest dist",
    "start": "ui5 serve"
  }
}
